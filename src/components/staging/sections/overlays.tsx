"use client";

import type { StagingContext } from "../types";

/** Placeholder: the overlay tools and list land in Task 15. */
export function OverlaysSection(props: { ctx: StagingContext }) {
  void props;
  return null;
}
