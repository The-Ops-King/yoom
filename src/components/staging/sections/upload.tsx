"use client";

import type { StagingContext } from "../types";

/** Placeholder: the render/upload trigger lands in Task 15. */
export function UploadSection(props: { ctx: StagingContext }) {
  void props;
  return null;
}
