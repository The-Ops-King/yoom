"use client";

import type { StagingContext } from "../types";

/** Placeholder: shape/size/mirror/offset controls land in Task 13. */
export function CameraSection(props: { ctx: StagingContext }) {
  void props;
  return null;
}
