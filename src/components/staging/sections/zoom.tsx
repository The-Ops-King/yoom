"use client";

import type { StagingContext } from "../types";

/** Placeholder: the zoom tool and clip list land in Task 14. */
export function ZoomSection(props: { ctx: StagingContext }) {
  void props;
  return null;
}
