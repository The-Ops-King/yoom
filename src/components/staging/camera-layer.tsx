"use client";

import type { StagingContext } from "./types";

/** Placeholder: the camera bubble drag/resize layer lands in Task 13. */
export function CameraLayer(props: { ctx: StagingContext }) {
  void props;
  return null;
}
