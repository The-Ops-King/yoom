"use client";

import type { StagingProps } from "./types";

export type { StagingProps } from "./types";

// Placeholder: the real staging screen lands in Task 12.
export function Staging(_props: StagingProps) {
  void _props;
  return <div>Staging</div>;
}
