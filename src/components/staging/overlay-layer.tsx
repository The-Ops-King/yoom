"use client";

import type { StagingContext } from "./types";

/** Placeholder: the overlay draw/drag layer lands in Task 15. */
export function OverlayLayer(props: { ctx: StagingContext }) {
  void props;
  return null;
}
