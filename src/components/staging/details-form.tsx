"use client";

import type { StagingContext } from "./types";

/** Placeholder: title/description/slug/thumbnail land in Task 15. */
export function DetailsForm(props: { ctx: StagingContext }) {
  void props;
  return null;
}
