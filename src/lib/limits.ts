/** Shared caps for user-entered video metadata, used by both the owner's
 * server actions (reject over cap) and the upload API (truncate to cap). */
export const MAX_TITLE = 200;
export const MAX_DESCRIPTION = 5000;
